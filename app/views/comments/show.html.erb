<p id="notice"><%= notice %></p>

<p>
  <strong>Content:</strong>
  <%= @comment.content %>
</p>

<p>
  <strong>Author(User):</strong>
  <%= @comment.user.email %>
</p>
<%# if current_user == @comment.user %>
<%= link_to 'Edit', edit_topic_post_comment_path(@topic,@post,@comment) %> |
  <%#end %>
<%= link_to 'Back', topic_post_comments_path %>
<%#= link_to 'rating', topic_post_comment_ratings_path(@topic,@post,@comment) %>
